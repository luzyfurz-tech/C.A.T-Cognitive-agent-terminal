[Unit]
Description=Cognitive Agent Terminal (C.A.T)
After=network.target ollama.service

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/cat
ExecStart=/usr/bin/npm start
Restart=on-failure
Environment=NODE_ENV=production PORT=3939

[Install]
WantedBy=multi-user.target
